# Supabase PostgreSQL
# 從 Supabase: Connect → ORM → Prisma 取得；只填在 .env.local。
DATABASE_URL=
DIRECT_URL=

# 網站與管理員 session（後續登入功能會使用）
NEXT_PUBLIC_APP_URL=http://localhost:3000
AUTH_SECRET=

# LINE（後續串接時使用）
LINE_CHANNEL_ID=
LINE_CHANNEL_SECRET=
LINE_CHANNEL_ACCESS_TOKEN=
NEXT_PUBLIC_LIFF_ID=
